const Valvulas_chevrolet = [
    { id: 230, nombre: "Valvulas Astra", precio: 3200, imagen: "logo_chevrolet.jpg" },
    { id: 231, nombre: "Valvulas Agile", precio: 3800, imagen: "logo_chevrolet.jpg" },
    { id: 232, nombre: "Valvulas Blazer", precio: 4600, imagen: "logo_chevrolet.jpg" },
    { id: 233, nombre: "Valvulas Captiva", precio: 4900, imagen: "logo_chevrolet.jpg" },
    { id: 234, nombre: "Valvulas Corsa", precio: 3100, imagen: "logo_chevrolet.jpg" },
    { id: 235, nombre: "Valvulas Vectra", precio: 3700, imagen: "logo_chevrolet.jpg" },
  ];
  const Valvulas_citroen = [
    { id: 236, nombre: "Valvulas berlingo", precio: 3700, imagen: "logo citroen.png" },
    { id: 237, nombre: "Valvulas c3", precio: 3200, imagen: "logo citroen.png" },
    { id: 238, nombre: "Valvulas c4", precio: 3800, imagen: "logo citroen.png" },
    { id: 239, nombre: "Valvulas c4 aircross", precio: 3100, imagen: "logo citroen.png" },
    { id: 240, nombre: "Valvulas cactus", precio: 4600, imagen: "logo citroen.png" },
    { id: 241, nombre: "Valvulas xsara picasso", precio: 4900, imagen: "logo citroen.png" },
  ];
  const Valvulas_fiat = [
    { id: 242, nombre: "Valvulas 128", precio: 3200, imagen: "logo fiat.jpg" },
    { id: 243, nombre: "Valvulas adventure", precio: 4900, imagen: "logo fiat.jpg" },
    { id: 244, nombre: "Valvulas duna", precio: 4600, imagen: "logo fiat.jpg" },
    { id: 245, nombre: "Valvulas fiorino", precio: 3100, imagen: "logo fiat.jpg" },
    { id: 246, nombre: "Valvulas toro", precio: 3700, imagen: "logo fiat.jpg" },
    { id: 247, nombre: "Valvulas uno", precio: 3800, imagen: "logo fiat.jpg" },
  ]; 
  const Valvulas_ford = [ 
    { id: 248, nombre: "Valvulas escort", precio: 3100, imagen: "logo ford.png" },
    { id: 249, nombre: "Valvulas fiesta", precio: 3200, imagen: "logo ford.png" },
    { id: 250, nombre: "Valvulas focus", precio: 3800, imagen: "logo ford.png" },
    { id: 251, nombre: "Valvulas ka", precio: 4600, imagen: "logo ford.png" },
    { id: 252, nombre: "Valvulas kuga", precio: 3700, imagen: "logo ford.png" },
    { id: 253, nombre: "Valvulas mondeo", precio: 4900, imagen: "logo ford.png" },
  ]; 
  const Valvulas_honda = [
    { id: 254, nombre: "Valvulas accord", precio: 3200, imagen: "logo honda.jpg" },
    { id: 255, nombre: "Valvulas civic", precio: 3800, imagen: "logo honda.jpg" },
    { id: 256, nombre: "Valvulas crv", precio: 4600, imagen: "logo honda.jpg" },
    { id: 257, nombre: "Valvulas integra", precio: 4900, imagen: "logo honda.jpg" },
  ];
  const Valvulas_nissan = [
    { id: 258, nombre: "Valvulas frontier", precio: 3200, imagen: "logo nissan.jpg" },
    { id: 259, nombre: "Valvulas sentra", precio: 3800, imagen: "logo nissan.jpg" },
    { id: 260, nombre: "Valvulas serena", precio: 4600, imagen: "logo nissan.jpg" },
    { id: 261, nombre: "Valvulas tiida", precio: 4900, imagen: "logo nissan.jpg" },
    { id: 262, nombre: "Valvulas pathfinder", precio: 3100, imagen: "logo nissan.jpg" },
  ];
  const Valvulas_peugeot = [
    { id: 263, nombre: "Valvulas 106", precio: 3200, imagen: "logo peugeot.png" },
    { id: 264, nombre: "Valvulas 205", precio: 3800, imagen: "logo peugeot.png" },
    { id: 265, nombre: "Valvulas 206", precio: 4600, imagen: "logo peugeot.png" },
    { id: 266, nombre: "Valvulas 207", precio: 4900, imagen: "logo peugeot.png" },
    { id: 267, nombre: "Valvulas 2008", precio: 3100, imagen: "logo peugeot.png" },
    { id: 268, nombre: "Valvulas 3008", precio: 3700, imagen: "logo peugeot.png" },
  ]; 
  const Valvulas_renault = [
    { id: 269, nombre: "Valvulas 12", precio: 3200, imagen: "logo renault.jpg" },
    { id: 270, nombre: "Valvulas clio", precio: 3800, imagen: "logo renault.jpg" },
    { id: 271, nombre: "Valvulas duster", precio: 4600, imagen: "logo renault.jpg" },
    { id: 272, nombre: "Valvulas megane", precio: 4900, imagen: "logo renault.jpg" },
    { id: 273, nombre: "Valvulas sandero", precio: 3100, imagen: "logo renault.jpg" },
    { id: 274, nombre: "Valvulas twingo", precio: 3700, imagen: "logo renault.jpg" },
  ];
  const Valvulas_toyota =  [
    { id: 275, nombre: "Valvulas celica", precio: 3200, imagen: "logo toyota.jpg" },
    { id: 276, nombre: "Valvulas corolla", precio: 3800, imagen: "logo toyota.jpg" },
    { id: 277, nombre: "Valvulas cresida", precio: 4600, imagen: "logo toyota.jpg" },
    { id: 278, nombre: "Valvulas etios", precio: 4900, imagen: "logo toyota.jpg" },
    { id: 279, nombre: "Valvulas hilux", precio: 3100, imagen: "logo toyota.jpg" },
    { id: 280, nombre: "Valvulas landcrusier", precio: 3700, imagen: "logo toyota.jpg" },
  ];
  const Valvulas_volkswagen = [
    { id: 281, nombre: "Valvulas bora", precio: 3200, imagen: "logo volkswagen.jpg" },
    { id: 282, nombre: "Valvulas fox", precio: 3800, imagen: "logo volkswagen.jpg" },
    { id: 283, nombre: "Valvulas gacel", precio: 4600, imagen: "logo volkswagen.jpg" },
    { id: 284, nombre: "Valvulas gol", precio: 4900, imagen: "logo volkswagen.jpg" },
    { id: 285, nombre: "Valvulas golf", precio: 3100, imagen: "logo volkswagen.jpg" },
    { id: 286, nombre: "Valvulas Vento", precio: 3700, imagen: "logo volkswagen.jpg" },
  ];

  //LocalStorage JUNTAS

function GuardarValvulaschevroletLS(Valvulas_chevrolet) {
    localStorage.setItem("Valvulas_chevrolet", JSON.stringify(Valvulas_chevrolet));
  }
  function CargarValvulasChevroletLS() {
    return JSON.parse(localStorage.getItem("Valvulas_chevrolet")) || [];
  }
  GuardarValvulaschevroletLS(Valvulas_chevrolet);
  
  function GuardarValvulasCitroenLS(Valvulas_citroen) {
    localStorage.setItem("Valvulas_citroen", JSON.stringify(Valvulas_citroen));
  }
  function CargarValvulasCitroenLS() {
    return JSON.parse(localStorage.getItem("Valvulas_citroen")) || [];
  }
  GuardarValvulasCitroenLS(Valvulas_citroen);
  
  function GuardarValvulasFiatLS(Valvulas_fiat) {
    localStorage.setItem("Valvulas_fiat", JSON.stringify(Valvulas_fiat));
  }
  function CargarValvulasFiatLS() {
    return JSON.parse(localStorage.getItem("Valvulas_fiat")) || [];
  }
  GuardarValvulasFiatLS(Valvulas_fiat);
  
  function GuardarValvulasFordLS(Valvulas_ford) {
    localStorage.setItem("Valvulas_ford", JSON.stringify(Valvulas_ford));
  }
  function CargarValvulasFordLS() {
    return JSON.parse(localStorage.getItem("Valvulas_ford")) || [];
  }
  GuardarValvulasFordLS(Valvulas_ford);
  
  function GuardarValvulasHondaLS(Valvulas_honda) {
    localStorage.setItem("Valvulas_honda", JSON.stringify(Valvulas_honda));
  }
  function CargarValvulasHondaLS() {
    return JSON.parse(localStorage.getItem("Valvulas_honda")) || [];
  }
  GuardarValvulasHondaLS(Valvulas_honda);
  
  function GuardarValvulasNissanLS(Valvulas_nissan) {
    localStorage.setItem("Valvulas_nissan", JSON.stringify(Valvulas_nissan));
  }
  function CargarValvulasNissanLS() {
    return JSON.parse(localStorage.getItem("Valvulas_nissan")) || [];
  }
  GuardarValvulasNissanLS(Valvulas_nissan);
  
  function GuardarValvulasPeugeotLS(Valvulas_peugeot) {
    localStorage.setItem("Valvulas_peugeot", JSON.stringify(Valvulas_peugeot));
  }
  function CargarValvulasPeugeotLS() {
    return JSON.parse(localStorage.getItem("Valvulas_peugeot")) || [];
  }
  GuardarValvulasPeugeotLS(Valvulas_peugeot);
  
  function GuardarValvulasRenaultLS(Valvulas_renault) {
    localStorage.setItem("Valvulas_renault", JSON.stringify(Valvulas_renault));
  }
  function CargarValvulasRenaultLS() {
    return JSON.parse(localStorage.getItem("Valvulas_renault")) || [];
  }
  GuardarValvulasRenaultLS(Valvulas_renault);
  
  function GuardarValvulasToyotaLS(Valvulas_toyota) {
    localStorage.setItem("Valvulas_toyota", JSON.stringify(Valvulas_toyota));
  }
  function CargarValvulasToyotaLS() {
    return JSON.parse(localStorage.getItem("Valvulas_toyota")) || [];
  }
  GuardarValvulasToyotaLS(Valvulas_toyota);
  
  function GuardarValvulasVolkswagenLS(Valvulas_volkswagen) {
    localStorage.setItem("Valvulas_volkswagen", JSON.stringify(Valvulas_volkswagen));
  }
  function CargarValvulasVolkswagenLS() {
    return JSON.parse(localStorage.getItem("Valvulas_volkswagen")) || [];
  }
  GuardarValvulasVolkswagenLS(Valvulas_volkswagen);


  //carrito chevrolet
  function AlertVacio(){
    Swal.fire({
      icon: 'info',
      title: 'Oops...',
      text: 'Tu carrito esta vacio!',
    })
  }

  function VaciarCarritoValvulasChevrolet(){
    localStorage.removeItem("carrito");
    MostrarProductosValvulasCarrito();
  ActualizarValvulasChevrolet();
  }
function GuardarValvulasChevroletCarrito(Valvulas_chevrolet) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_chevrolet));
  }
  function CargarValvulasChevroletCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasChevrolet(id) {
    const Valvulas_chevrolet = CargarValvulasChevroletLS();
    return Valvulas_chevrolet.find((elemento) => elemento.id == id);
  }
  function BuscarObjetoValvulasChevrolet(id) {
    const Valvulas_chevrolet = CargarValvulasChevroletLS();
    return Valvulas_chevrolet.find((elemento) => elemento.id == id);
  }

  function AgregarAlCarritoValvulasChevrolet(id) {
    const productos_carrito = CargarValvulasChevroletCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
     if(posicion > -1) {
        productos_carrito[posicion].cantidad += 1;
     }else{
        const objeto = BuscarObjetoValvulasChevrolet(id);
        objeto.cantidad = 1;
        productos_carrito.push(objeto);
     }   
     Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    
    GuardarValvulasChevroletCarrito(productos_carrito);
    ActualizarValvulasChevrolet();
   
  }

  function AgregarItemAlCarrito(id) {
    const productos_carrito = CargarValvulasChevroletCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
     if(posicion > -1) {
        productos_carrito[posicion].cantidad += 1;
     }else{
        const objeto = BuscarObjetoValvulasChevrolet(id);
        objeto.cantidad = 1;
        productos_carrito.push(objeto);
     }   
    
    GuardarValvulasChevroletCarrito(productos_carrito);
    MostrarProductosValvulasCarrito();
    ActualizarValvulasChevrolet();
   
  }
  function SumatotaldeProductos(){
    const productos_carrito = CargarValvulasChevroletCarrito();

    return productos_carrito.reduce((acumulador, item) => acumulador + item.cantidad, 0)
  }

  function SumatotalaPagar(){
    const productos_carrito = CargarValvulasChevroletCarrito();

    return productos_carrito.reduce((acumulador, item) => acumulador + (item.cantidad * item.precio),0)
  }

  function AgregarItem(id){
    AgregarAlCarritoValvulasChevrolet(id);
    MostrarProductosValvulasCarrito();
  }

  function EliminardelCarritoValvulasChevrolet(id) {
    const productos_carrito = CargarValvulasChevroletCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    productos_carrito[posicion].cantidad -=1;
     if(productos_carrito[posicion].cantidad == 0) {
        productos_carrito.splice(posicion, 1);
     }    
     GuardarValvulasChevroletCarrito(productos_carrito);
     MostrarProductosValvulasCarrito();
    ActualizarValvulasChevrolet();
  }
  
  function ActualizarValvulasChevrolet() {
    let chevrolet = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = chevrolet;
  }
  ActualizarValvulasChevrolet();
  
  
  //carrito citroen
  
  function GuardarValvulasCitroenCarrito(Valvulas_citroen) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_citroen));
  }
  function CargarValvulasCitroenCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasCitroen(id) {
    const Valvulas_citroen = CargarValvulasCitroenLS();
    return Valvulas_citroen.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasCitroen(id) {
    const productos_carrito = CargarValvulasCitroenCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
     if(posicion > -1) {
        productos_carrito[posicion].cantidad += 1;
     }else{
        const objeto = BuscarObjetoValvulasCitroen(id);
        objeto.cantidad = 1;
        productos_carrito.push(objeto);
     }
     Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasCitroenCarrito(productos_carrito);
    ActualizarValvulasCitroen();
  }
  
  function ActualizarValvulasCitroen() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasCitroen();
  
  //carrito fiat
  
  function GuardarValvulasFiatCarrito(Valvulas_fiat) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_fiat));
  }
  function CargarValvulasFiatCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasFiat(id) {
    const Valvulas_fiat = CargarValvulasFiatLS();
    return Valvulas_fiat.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasFiat(id) {
    const productos_carrito = CargarValvulasFiatCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasFiat(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasFiatCarrito(productos_carrito);
    ActualizarValvulasFiat();
  }
  
  function ActualizarValvulasFiat() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasFiat();
  
  //carrito ford
  
  function GuardarValvulasFordCarrito(Valvulas_ford) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_ford));
  }
  function CargarValvulasFordCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasFord(id) {
    const Valvulas_ford = CargarValvulasFordLS();
    return Valvulas_ford.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasFord(id) {
    const productos_carrito = CargarValvulasFordCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasFord(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasFordCarrito(productos_carrito);
    ActualizarValvulasFord();
  }
  
  function ActualizarValvulasFord() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasFord();
  
  //carrito honda
  
  function GuardarValvulasHondaCarrito(Valvulas_honda) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_honda));
  }
  function CargarValvulasHondaCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasHonda(id) {
    const Valvulas_honda = CargarValvulasHondaLS();
    return Valvulas_honda.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasHonda(id) {
    const productos_carrito = CargarValvulasHondaCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasHonda(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasHondaCarrito(productos_carrito);
    ActualizarValvulasHonda();
  }
  
  function ActualizarValvulasHonda() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasHonda();
  
  //carrito nissan
  
  function GuardarValvulasNissanCarrito(Valvulas_nissan) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_nissan));
  }
  function CargarValvulasNissanCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasNissan(id) {
    const Valvulas_nissan = CargarValvulasNissanLS();
    return Valvulas_nissan.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasNissan(id) {
    const productos_carrito = CargarValvulasNissanCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasNissan(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasNissanCarrito(productos_carrito);
    ActualizarValvulasNissan();
  }
  
  function ActualizarValvulasNissan() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasNissan();
  
  //carrito peugeot
  
  function GuardarValvulasPeugeotCarrito(Valvulas_peugeot) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_peugeot));
  }
  function CargarValvulasPeugeotCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasPeugeot(id) {
    const Valvulas_peugeot = CargarValvulasPeugeotLS();
    return Valvulas_peugeot.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasPeugeot(id) {
    const productos_carrito = CargarValvulasPeugeotCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasPeugeot(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasPeugeotCarrito(productos_carrito);
    ActualizarValvulasPeugeot();
  }
  
  function ActualizarValvulasPeugeot() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasPeugeot();
  
  //carrito renault
  
  function GuardarValvulasRenaultCarrito(Valvulas_renault) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_renault));
  }
  function CargarValvulasRenaultCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasRenault(id) {
    const Valvulas_renault = CargarValvulasRenaultLS();
    return Valvulas_renault.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasRenault(id) {
    const productos_carrito = CargarValvulasRenaultCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasRenault(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasRenaultCarrito(productos_carrito);
    ActualizarValvulasRenault();
  }
  
  function ActualizarValvulasRenault() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasRenault();
  
  //carrito toyota
  
  function GuardarValvulasToyotaCarrito(Valvulas_toyota) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_toyota));
  }
  function CargarValvulasToyotaCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasToyota(id) {
    const Valvulas_toyota = CargarValvulasToyotaLS();
    return Valvulas_toyota.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasToyota(id) {
    const productos_carrito = CargarValvulasToyotaCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasToyota(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasToyotaCarrito(productos_carrito);
    ActualizarValvulasToyota();
  }
  
  function ActualizarValvulasToyota() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasToyota();
  
  //carrito volkswagen
  
  function GuardarValvulasVolkswagenCarrito(Valvulas_volkswagen) {
    localStorage.setItem("carrito", JSON.stringify(Valvulas_volkswagen));
  }
  function CargarValvulasVolkswagenCarrito() {
    return JSON.parse(localStorage.getItem("carrito")) || [];
  }
  
  function BuscarObjetoValvulasVolkswagen(id) {
    const Valvulas_volkswagen = CargarValvulasVolkswagenLS();
    return Valvulas_volkswagen.find((elemento) => elemento.id == id);
  }
  function AgregarAlCarritoValvulasVolkswagen(id) {
    const productos_carrito = CargarValvulasVolkswagenCarrito();
    let posicion = productos_carrito.findIndex(item =>item.id === id)
    if(posicion > -1) {
       productos_carrito[posicion].cantidad += 1;
    }else{
       const objeto = BuscarObjetoValvulasVolkswagen(id);
       objeto.cantidad = 1;
       productos_carrito.push(objeto);
    } 
    Swal.fire({
      icon: 'success',
      title: 'Felicitaciones!!',
      text: 'Producto agregado correctamente al carrito ',
    })
    GuardarValvulasVolkswagenCarrito(productos_carrito);
    ActualizarValvulasVolkswagen();
  }
  
  function ActualizarValvulasVolkswagen() {
    let contenido = `<button type="button" class="btn btn-primary position-relative">
    <img src="../img/carrito.jpg" alt="Carrito" widht="15" height="50" > 
    <span class="position-absolute top-0 start-50 translate-middle badge rounded-pill bg-danger">${SumatotaldeProductos()}</span>
  </button>`;
    document.getElementById("carrito").innerHTML = contenido;
  }
  ActualizarValvulasVolkswagen();
  